<h1><%= @article.title %> </h1>

<p><%= tag_links(@article.tags)%></p>

<p>
  <% if @article.image.exists? %>
  <%= image_tag @article.image.url %>
  <% end %>
</p>

<p><%= @article.body %> </p>

<h3>People's Thoughts(<%= @article.comments.size %>)</h3>
<%= render partial: 'comment', collection: @article.comments %>

<% if logged_in? %>
  <h3><%= link_to "Edit This Article", edit_article_path(@article) %></h3>

  <h4><%= link_to "Delete This Article", article_path(@article), method: :delete, confirm: "Really delete this piece of knowledge?" %></h4>
<% end %>

<%= render partial: 'comments/form' %>

<h3><%= link_to "Back To Main Page", articles_path %></h3>
